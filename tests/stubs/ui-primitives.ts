/**
 * Test stub for `@deepseek-ai/dsh-client-ui-primitives` — the shell's static
 * module table resolves the real one; controller tests only need the native
 * chevron the card's header renders.
 */

export function IconChevronDownOutline14(_props: { size?: number; className?: string }): unknown {
  return null
}
